# AGENT.md — moved to AGENTS.md

This file's content moved to [`AGENTS.md`](./AGENTS.md) as part of setting up a full Intent Layer (hierarchical `AGENTS.md` nodes under `Source/*`). `AGENTS.md` is the cross-tool standard name and is now the canonical root.

This stub is kept in place (rather than deleted) only because in-code comments across the codebase still say "see AGENT.md" by that exact filename (e.g. `Source/UI/TouchSizing.h`, `Source/UI/OverlayHost.h`, `Source/UI/MainComponent.h`, `Source/Effects/NAMProcessor.h`). Those references still resolve correctly to the project's guidance — just via this redirect. New code comments should reference `AGENTS.md` going forward.

See [`AGENTS.md`](./AGENTS.md) for the root Intent Layer node, and its Downlinks table for the per-directory `AGENTS.md` nodes under `Source/`.
